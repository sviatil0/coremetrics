name: C/C++ CI

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main", "dev" ]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        # Linux + macOS are the gated targets. Windows is now added so the
        # Windows metrics layer (SystemMetrics_win.cpp) stays compile-clean,
        # but the Windows leg is marked continue-on-error because the
        # SDL3 + pkg-config toolchain on the runner is still flaky. The
        # PR-gating signal stays driven by Linux + macOS.
        include:
          - os: ubuntu-latest
            allow-failure: false
          - os: macos-latest
            allow-failure: false
          - os: windows-latest
            allow-failure: true

    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.allow-failure }}

    steps:
    - uses: actions/checkout@v4

    - name: Install dependencies (Linux)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential pkg-config libfreetype-dev libharfbuzz-dev

    - name: Install dependencies (macOS)
      if: runner.os == 'macOS'
      run: |
        brew update
        brew install sdl3 sdl3_ttf sdl3_image

    - name: Setup SDL3 (Linux)
      if: runner.os == 'Linux'
      uses: libsdl-org/setup-sdl@main
      with:
        install-linux-dependencies: true
        version: 3-latest
        version-sdl-ttf: 3-latest
        version-sdl-image: 3-latest

    - name: Setup SDL3 (Windows)
      if: runner.os == 'Windows'
      uses: libsdl-org/setup-sdl@main
      with:
        version: 3-latest
        version-sdl-ttf: 3-latest
        version-sdl-image: 3-latest

    - name: Build tests (Linux)
      if: runner.os == 'Linux'
      env:
        SDL_VIDEO_DRIVER: dummy
      run: |
        LIB_PATHS=$(pkg-config --libs-only-L sdl3 sdl3-ttf sdl3-image | sed 's|-L||g')
        for P in $LIB_PATHS; do export LD_LIBRARY_PATH="$P:$LD_LIBRARY_PATH"; done
        RPATHS=""
        for P in $LIB_PATHS; do RPATHS="$RPATHS -Wl,-rpath,$P"; done
        make CXXFLAGS="-std=c++23 -Wall -I ./include $(pkg-config --cflags sdl3 sdl3-ttf sdl3-image)" \
             LDFLAGS="$(pkg-config --libs sdl3 sdl3-ttf sdl3-image) $RPATHS" test

    - name: Build tests (macOS)
      if: runner.os == 'macOS'
      env:
        SDL_VIDEO_DRIVER: dummy
      run: |
        make test

    - name: Build tests (Windows)
      if: runner.os == 'Windows'
      shell: bash
      env:
        SDL_VIDEO_DRIVER: dummy
      run: |
        which pkg-config || pacman -S --noconfirm mingw-w64-x86_64-pkgconf || true
        echo "PKG_CONFIG_PATH=$PKG_CONFIG_PATH"
        pkg-config --cflags sdl3 sdl3-ttf sdl3-image || true
        make CXX=g++ \
             CXXFLAGS="-std=c++23 -Wall -I ./include $(pkg-config --cflags sdl3 sdl3-ttf sdl3-image)" \
             LDFLAGS="$(pkg-config --libs sdl3 sdl3-ttf sdl3-image)" \
             test

  sanitizers:
    # Re-run the test suite under AddressSanitizer and UndefinedBehaviorSanitizer
    # on Linux. Non-blocking for now (continue-on-error: true) so a sanitizer
    # report doesn't gate a merge while the targets bed in; flip to required
    # once the suite has stayed clean for a release cycle.
    needs: build
    runs-on: ubuntu-latest
    continue-on-error: true

    steps:
    - uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential pkg-config libfreetype-dev libharfbuzz-dev

    - name: Setup SDL3
      uses: libsdl-org/setup-sdl@main
      with:
        install-linux-dependencies: true
        version: 3-latest
        version-sdl-ttf: 3-latest
        version-sdl-image: 3-latest

    - name: ASan tests
      env:
        SDL_VIDEO_DRIVER: dummy
        ASAN_OPTIONS: detect_leaks=0:halt_on_error=1
      run: |
        LIB_PATHS=$(pkg-config --libs-only-L sdl3 sdl3-ttf sdl3-image | sed 's|-L||g')
        for P in $LIB_PATHS; do export LD_LIBRARY_PATH="$P:$LD_LIBRARY_PATH"; done
        RPATHS=""
        for P in $LIB_PATHS; do RPATHS="$RPATHS -Wl,-rpath,$P"; done
        make asan CXXFLAGS="-std=c++23 -Wall -I ./include $(pkg-config --cflags sdl3 sdl3-ttf sdl3-image)" \
                  LDFLAGS="$(pkg-config --libs sdl3 sdl3-ttf sdl3-image) $RPATHS"

    - name: UBSan tests
      env:
        SDL_VIDEO_DRIVER: dummy
        UBSAN_OPTIONS: halt_on_error=1:print_stacktrace=1
      run: |
        LIB_PATHS=$(pkg-config --libs-only-L sdl3 sdl3-ttf sdl3-image | sed 's|-L||g')
        for P in $LIB_PATHS; do export LD_LIBRARY_PATH="$P:$LD_LIBRARY_PATH"; done
        RPATHS=""
        for P in $LIB_PATHS; do RPATHS="$RPATHS -Wl,-rpath,$P"; done
        make ubsan CXXFLAGS="-std=c++23 -Wall -I ./include $(pkg-config --cflags sdl3 sdl3-ttf sdl3-image)" \
                   LDFLAGS="$(pkg-config --libs sdl3 sdl3-ttf sdl3-image) $RPATHS"
